# Copy to backend/.env for local overrides. Nothing here is secret.
# Note: the server does not currently load dotenv — only PORT is read,
# and Render injects that itself. This file is documentation only.

PORT=4000
